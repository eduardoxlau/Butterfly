{
  "name": "butterfly",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "lerna run dev --parallel",
    "db:seeds": "lerna run db:seeds --parallel",
    "test": "lerna run jest"
  },
  "dependencies": {
    "lerna": "^6.1.0"
  }
}
